Generate a complete Rails model for a [resource type] with the following attributes:
[List attributes with types]

It should have these associations:
[List associations]

Include validations for:
[List validation requirements]

Also create the database migration with appropriate indexes and foreign keys.